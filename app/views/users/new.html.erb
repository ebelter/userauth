<% provide(:title, 'Sign Up') %>
<h1>Sign Up!</h1>

<div id="registration-restricted" STYLE="color: #ff6666; text-align: center">This app is currently in development, and new user registratons are restricted to invite only.</div>

<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= form_for(@user) do |f| %>
			<%= render 'form_fields', f: f %>
			<%= f.submit "Create Account", class: "btn btn-primary" %>
		<% end %>
	</div>
</div>
